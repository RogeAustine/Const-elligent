# 验收记录

验收日期：2026-09-20。测试环境：Windows、Python 3.13.9、Node.js 24.16.0（仅用于 JavaScript 语法检查）。

## 自动化验证

运行 `python -m unittest discover -s tests -q`：33 项测试全部通过，最终运行约 1.5 秒。

覆盖场景：

- 合成异常数据识别出 PV-03 发电偏低、PV-06 转换比值偏低。
- 阴云使绝对功率下降时不误创建异常工单。
- 低辐照、样本不足、时间间断、过期数据、单次偏差进入观察。
- CSV 正常往返；拒绝缺列、空数据、重复样本、NaN/Inf、负值、错误功率口径和额定功率变化。
- 全站与指定 PV 编号检查；拒绝不存在的 PV 编号。
- 工具记录与原始证据实际保存，不因用户任务文字或模型解释而自动批准工单。
- 同一草案重复确认返回同一工单，跨检查冒用草案和重复的未关闭工单被拒绝。
- 模拟复测通过后关闭工单；未通过保持待处理；旧时间数据不允许复测。
- 导入数据的工单不能使用模拟数据关闭。
- 可选模型失败时，本地结果与草案保留；模型解释不能改变审批状态。
- 服务重启后保留数据，并把中断的检查标记为失败。
- HTTP 接口端到端完成检查、确认、复测、报告导出。
- 随附 inspection.csv 导入与 retest.csv 复测闭环通过。
- 同源令牌校验、静态路径保护、文件类型与 CSP 响应头通过。
- 同一端口不能启动第二个实例；架构页面内联样式有对应 CSP nonce。

`node --check web/app.js` 通过。

`python -m py_compile app.py agent.py store.py telemetry.py model_client.py` 通过。

`start.ps1 --help` 与 `start.bat --help` 启动入口通过，能找到实际可运行且版本满足要求的 Python。

## 浏览器验收

使用本地应用的浏览器页面完成并核对：

1. 工作台初始布局与模拟数据标识。
2. 点击检查后显示真实执行状态与五条工具记录。
3. 展示两份对应不同偏差类型的草案及 KB 引用入口。
4. 通过确认对话框以“浏览器验收角色”生成演示工单。
5. 在工单页面模拟恢复、复测，最终显示“复测通过 · 已关闭”。

浏览器验收记录保留在本地数据库，便于直接查看完整闭环；它们不代表真实工程维修。最终重启后 HTTP 核对：检查记录 1 条、工单 1 条仍存在，架构页面返回 200。

## 交付范围与未验证项

此次成果只使用已确认的赛事与申报书内容独立实现，未参考其他项目代码。源代码、知识条目、测试、样例及文档位于同一交付目录；运行依赖仅为 Python 标准库和浏览器。

未使用真实模型账号发起付费调用；模型成功与失败行为通过替身测试验证，实际服务商兼容性需配置后检查。未接入真实 BIPV、BIM、C-SMART 或现场传感器，未评估工程准确率与节能收益。未完成独立移动设备验收或架构页的实体打印验证。
